export const generateMetadata = async () => {
    return {
        title: "Cast - Dashboard",
        description: "Manage cast",
    }
}

const page = () => {
    return (
        <div>page</div>
    )
}

export default page