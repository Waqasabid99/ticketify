import Register from "@/components/auth/Register"

export const metadata = {
    title: "Register",
    description: "Create new account."
}

const page = () => {
    return (
        <Register />
    )
}

export default page